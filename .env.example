# ─── Required for production ───────────────────────────────────────

# Strong password for /admin login (required on Vercel)
ADMIN_PASSWORD=your-secure-password-here

# Public site URL (used for sitemap, Open Graph, canonical links)
NEXT_PUBLIC_SITE_URL=https://your-domain.com

# ─── Required for lead persistence on Vercel ───────────────────────
# Vercel serverless functions have no persistent filesystem.
# Create a Blob store in your Vercel project dashboard:
#   Storage → Blob → Create Store
# Then pull env vars:  vercel env pull
BLOB_READ_WRITE_TOKEN=

# ─── Local development only ──────────────────────────────────────
# Without BLOB_READ_WRITE_TOKEN, leads are saved to data/leads.json locally.
# Default dev admin password (when ADMIN_PASSWORD is unset): vevadeco2026
